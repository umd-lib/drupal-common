<?php

/**
 * The .module file is falling out of favor in Drupals 8 and 9
 * but still has some uses. It was the primary file in 7 and earlier.
 */

/**
 * Implement hook_cron
 */
function aleph_connector_cron() {
  $aleph_controller = new \Drupal\aleph_connector\Controller\AlephController();
  $aleph_controller->updateEquipmentDataCache();
}
