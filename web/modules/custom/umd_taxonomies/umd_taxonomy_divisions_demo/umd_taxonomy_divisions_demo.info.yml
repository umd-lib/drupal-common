name: UMD Library Divisions Demo Data
description: Demo data for umd_taxonomy_divisions
# Drush will export the UUIDs under content/, which will then import when the module
# is enabled. Note that this module must be enabled to run the drush command.
# e.g.,
# > drush dcem umd_content_example

package: 'UMD Taxonomies Demo Content'
dependencies:
  - umd_taxonomy_divisions
type: module

core_version_requirement: ^11 || ^9 || ^10
