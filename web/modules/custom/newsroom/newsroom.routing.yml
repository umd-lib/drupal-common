newsroom.news:
  path: '/about/news/{year_month}/{title}'
  requirements:
    _permission: 'access content'
    year_month: '^$|[^/]++'
    title: '^$|[^/]++'
  options:
    parameters:
      year_month:
        type: String
      title:
        type: String
