name: UMD Solr Defaults
description: A helper module for setting up Solr locally. Should be expanded as new Solr databases come online.

package: 'UMD Services'
dependencies:
  - search_api_solr
  - easy_install
type: module

core_version_requirement: ^8.9 || ^9 || ^10
